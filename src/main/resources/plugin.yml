name: NoKnockback # the plugins name as it should appear in the plugin list /pl
version: 1.0 # the plugin's version
author: Nodoka4318
main: ml.heartfulcpvp.noknockback.NoKnockback
api-version:
depend: [ProtocolLib]

commands:
  autocrystal:
    description: toggle
    usage: /noknockback